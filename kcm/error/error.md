| 错误 | 表象 | 根因（设计/概念层） | 以后怎么避免 |
|------|------|---------------------|-------------|
| run()进入guard后报错 | allowed_dir为'',导致报错 | file_str为''，因为解析的是file_str而tool_args定义的是fileStr，因为TOOLS定义时写错了 | 当报错定义不到了，往调用上一层去查 |
| 文件路径方法错误 | 不懂路径的流程 | 底层的方法不熟悉，造成了这个问题。Path(str)把路径变成对象，.expanduser()去除~路径，变成真实路径，.resolve()是把相对路径变成绝对路径 | 熟悉底层的方法和连贯的动作链条 |
| 搜索文件错误 | 不懂搜索的流程 | 搜索底层的方法不熟悉，造成了这个问题。Path(str).expanduser().resolve().rblob(pattern)递归的去找对应的目录中的匹配的文件，包含子目录，blob(pattern)不包含子目录 | 熟悉底层的方法和连贯的动作链条 |
| type vs isinstance 混淆 | `type(x)==object` 以为能返回True；`isinstance(x, object)` 以为可能返回False | **根因：没搞清"精确类型"vs"继承关系"两个概念**<br><br>**type(x)** → 返回**精确类型**（如 `<class 'list'>`、`<class 'dict'>`），永远不会返回 `object`。用于判断"它到底是什么类型"<br>**isinstance(x, C)** → 判断 x 是 C 的实例 **或 C 的子类实例**。因为所有类都继承自 `object`，所以 `isinstance(任何东西, object)` **永远为 True**<br><br>类比：type = 身份证上的精确名称；isinstance = 问"你是不是某家族的人（含后代）" | 记住两条铁律：<br>① `type(x) == object` → **永远 False**<br>② `isinstance(x, object)` → **永远 True**<br>需要精确匹配用 type，需要兼容子类用 isinstance |
| .get() 用错对象/参数 | 对非 dict 调用 .get()；把变量名当 key 传进去；把 dict 本身当 key 传 | **根因：.get() 是 dict 专属方法，key 必须是字符串**<br><br>`dict.get(key, default)` — key 必须是**字符串字面量**如 `"name"`，不是变量名也不是 dict 本身<br>嵌套取值要链式调用：`d.get("a", {}).get("b", "")`<br>非 dict 对象（如自定义类的实例）要用 `getattr(obj, "attr", default)` | ① 先 `isinstance(data, dict)` 判断<br>② key 写成字符串 `"xxx"`<br>③ 嵌套用链式 `.get()`<br>④ 对象属性用 `getattr()` |
| dict['xxx'] vs dict.get('xxx') | 不知该用方括号还是 .get() | **根因：没理解"安全访问"vs"直接访问"的区别**<br><br>`dict['key']` → key 不存在时 **抛出 KeyError**，程序崩溃。适合确定 key 一定存在的场景<br>`dict.get('key', default)` → key 不存在时 **返回 default**（默认 None），不报错。适合不确定 key 是否存在的场景<br><br>业务场景：外部数据（API返回、用户输入）→ 用 .get()；内部确定的数据结构 → 可用 [] | 外部/不可信数据 → **优先用 `.get()`**<br>内部/确定存在的 key → **可用 `[]`** |
| getattr vs 方括号 vs 点号 | 三种取值方式混用，不知道该用哪个 | **根因：没区分三种目标对象的类型**<br><br>| 语法 | 目标 | key不存在时 ||------|------|-------------|| `obj.attr` / `obj.xxx` | 对象属性 | 抛 AttributeError || `getattr(obj, 'attr', default)` | 对象属性（安全） | 返回 default || `dict['key']` | 字典 | 抛 KeyError || `dict.get('key', default)` | 字典（安全） | 返回 default |<br><br>**核心原则：先判断类型，再选方法** | 一句话：**是字典用 .get()/.[]，是对象用 getattr/点号** |
| 四大容器类型混淆 | 元组、list、dict、set 的特性和场景选错 | **根因：没掌握各容器的核心差异**<br><br>| 类型 | 可变 | 有序 | 可重复 | 取值方式 | 典型场景 ||------|------|------|--------|----------|----------|| **tuple** ( ) | ❌ 不可变 | ✅ 有序 | ✅ 可重复 | 索引 `t[0]` | 固定配置、函数多返回值、dict的key || **list** [ ] | ✅ 可变 | ✅ 有序 | ✅ 可重复 | 索引/切片/遍历 | 有序集合、序列操作、栈/队列 || **dict** { } | ✅ 值可变 | ✅ 3.7+有序 | ❌ key不可重复 | `d[key]` / `.get()` | 键值映射、JSON、工具定义 || **set** { } | ✅ 可变 | ❌ 无序 | ❌ 不可重复 | 遍历/`in`判断 | 去重、交集/并集、成员检测 |<br><br>**记忆口诀**：tuple定死了不改，list有序随便动，set自动去重不管顺序，dict一对一找value | 选型三问：需要改吗？需要顺序吗？需要去重吗？→ 对应选出容器 |
| join 三大坑 | ① `list.join()` 报 `AttributeError`<br>② `",".join([1,2,3])` 报 `TypeError`<br>③ `"/".join("abc")` → `a/b/c` 不是期望结果 | **根因：Python 的 join 语法和其他语言反着来，且有三个易错点**<br><br>**坑1：调用方写反了**（Java/JS 习惯带过来）<br>语法是 **`"分隔符".join(可迭代对象)`**，分隔符当主人，列表当客人<br>❌ `["a","b"].join(",")` → `AttributeError`，list 没有 join 方法<br>✅ `",".join(["a","b"])` → `'a,b'`<br><br>**坑2：列表中有非字符串元素**<br>`join()` 要求所有元素都是 str，int 不行<br>❌ `",".join([1, 2, 3])` → `TypeError: expected str`<br>✅ `",".join(str(n) for n in [1,2,3])` → `'1,2,3'`<br>✅ `",".join(map(str, [1,2,3]))`<br><br>**坑3：对字符串用 join → 被拆成单字符**<br>字符串也是可迭代对象，会逐个字符拆开<br>❌ `"-".join("abc")` → `'a-b-c'`（以为只加一个分隔符）<br>✅ 把字符串包在列表里：`"-".join(["abc"])` → `'abc'`<br><br>**对比 os.path.join**：这个是正常参数的函数，`os.path.join("a","b")` → `'a/b'`，不要和 `str.join` 搞混 | ① **永远写成** `"分隔符".join(列表)`<br>② **先确保列表元素全是 str**，有 int 就用 `map(str, ...)` 或生成器转<br>③ **别对裸字符串用 join**，要拼整个字符串就包成 `[str]`<br>④ **路径用 `os.path.join` / `Path()`，别用 `str.join`** |
| 12:| **RBAC — Casbin 集成** | — | **今日练习总结（2026-05-09）**<br><br>**一、Casbin 是什么**<br>Casbin = 一个配置驱动的权限判断引擎。把"谁能干什么"从代码里抽出来，写成 `.conf` + `.csv` 文件。代码只调 `enforce(sub, obj, act)`，返回 `True/False`。<br><br>**二、model.conf 五段结构**<br><br>| 段 | 作用 | 类比 |<br>|---|---|---|<br>| `[request_definition]` `r = sub, obj, act` | 定义每次 `enforce()` 传参的格式 | 函数签名 |<br>| `[policy_definition]` `p = sub, obj, act` | 定义 csv 里权限规则的表结构 | 数据库列定义 |<br>| `[role_definition]` `g = _, _` | 声明一个两列的角色映射表 | 花名册索引 |<br>| `[matchers]` `m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act` | 判断公式：请求和策略是否匹配 | if 条件 |<br>| `[policy_effect]` `e = some(where (p.eft == allow))` | 多条匹配时的裁决规则 | 最终裁决 |<br><br>**三、policy.csv 两种行**<br>`p, admin, data, read` ← 角色权限表：admin 角色能对 data 做 read<br>`g, kcm, admin` ← 角色分配表：kcm 这个人属于 admin 角色<br><br>`p` 行 = 给角色挂权限，`g` 行 = 给人挂角色。权限自动流转：人 → 角色 → 权限。<br><br>**四、加载与使用**<br>`self.enforcer = casbin.Enforcer('config/model.conf', 'config/policy.csv')`<br>`result = self.enforcer.enforce(user, resource, action)` → True/False<br><br>**五、拦截位置**<br>在 `run()` 中 LLM 返回后、`_execute_tool()` 之前拦截：<br>`action = TOOL_ACTION_MAP.get(tool_name, 'unknown')`<br>`if not self.enforcer.enforce(user, resource, action): ... continue`<br><br>**六、TOOL_ACTION_MAP 字典映射**<br>用一个值查另一个值的对照表，用 `.get(key, default)` 安全取值：<br>`"create_file"→"create"`, `"read_file"→"read"`, `"search_file"→"search"`, `"edit_file"→"update"`, `"run_bash"→"run_bash"`, `"tavily_search"→"online_search"`, `"run_python"→"run_python"`<br><br>**七、policy_effect 四种写法**<br>| 写法 | 含义 |<br>|---|---|<br>| `some(where (p.eft == allow))` | 有一条 allow 就放行（最常用） |<br>| `!some(where (p.eft == deny))` | 没人 deny 就放行 |<br>| `some(allow) && !some(deny)` | 有 allow 且没 deny 才放行（最严格） |<br>| `priority \|\| deny` | 按优先级裁决 |<br><br>**八、当前约束**<br>- `user` 从 `main.py` 传入，暂无登录层，默认 `'anonymous'`<br>- `resource` 目前写死 `'data'`，后续应从 `tool_args` 中动态提取文件路径<br>- `e2b_file` 不在 TOOL_ACTION_MAP 中 |
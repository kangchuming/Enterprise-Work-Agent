| 错误 | 表象 | 根因（设计/概念层） | 以后怎么避免 |
|------|------|---------------------|-------------|
| run()进入guard后报错 | allowed_dir为'',导致报错 | file_str为''，因为解析的是file_str而tool_args定义的是fileStr，因为TOOLS定义时写错了 | 当报错定义不到了，往调用上一层去查 |
